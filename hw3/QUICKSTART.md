# 🎬 Cinema Booking System - 快速開始

## ✅ 專案已完成初始化

### 📁 專案結構

```
hw3/
├── public/                 # 靜態資源
│   └── data/              # CSV 資料檔案
│       ├── movies.csv     # 10 部電影
│       ├── halls.csv      # 3 個影廳
│       └── screenings.csv # 168 個場次
├── src/
│   ├── components/        # React 組件
│   │   ├── ui/           # Shadcn UI 組件
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   └── badge.tsx
│   │   └── Navbar.tsx    # 導覽列組件
│   ├── pages/            # 頁面組件
│   │   ├── Home.tsx      # 首頁（已完成）
│   │   ├── Movies.tsx    # 電影列表（已完成）
│   │   └── Cart.tsx      # 購物車（已完成）
│   ├── context/          # 狀態管理
│   │   └── MovieContext.tsx  # 電影資料 Context
│   ├── hooks/            # 自定義 Hooks（待實作）
│   ├── utils/            # 工具函數（待實作）
│   ├── lib/
│   │   └── utils.ts      # cn() 工具函數
│   ├── App.tsx           # 主應用（含路由設定）
│   ├── main.tsx          # 入口文件
│   └── index.css         # 全域樣式（含 Tailwind）
└── 配置文件
    ├── package.json
    ├── vite.config.ts
    ├── tailwind.config.js
    ├── tsconfig.json
    └── .gitignore
```

## 🚀 如何執行

### 1. 開發伺服器已在背景執行
```bash
# 如果需要重新啟動：
npm run dev
```

### 2. 開啟瀏覽器
訪問：`http://localhost:5173`

### 3. 建構生產版本
```bash
npm run build
npm run preview
```

## 🎯 目前完成的功能

### ✅ 已實作
- [x] Vite + React + TypeScript 專案初始化
- [x] TailwindCSS 配置完成
- [x] Shadcn UI 組件整合（Button, Card, Badge）
- [x] 專案資料夾結構建立
- [x] React Router 路由設定
- [x] MovieContext 狀態管理
- [x] Navbar 導覽列
- [x] Home 首頁（含功能介紹）
- [x] Movies 電影列表頁（可載入 CSV 資料）
- [x] Cart 購物車頁（基礎結構）
- [x] CSV 資料檔案（電影、影廳、場次）

### 🔨 待實作功能（稍後階段）
- [ ] MovieDetail 電影詳細資訊頁
- [ ] SeatSelection 座位選擇功能
- [ ] 購物車完整功能（增刪改查）
- [ ] Checkout 結帳確認頁
- [ ] 電影搜尋與篩選
- [ ] 場次時間篩選
- [ ] 座位圖視覺化
- [ ] 訂單確認與顯示

## 📝 技術細節

### 使用的套件
- **react** & **react-dom** - React 18
- **react-router-dom** - 路由管理
- **papaparse** - CSV 解析
- **@radix-ui/** - 無障礙 UI 原件
- **class-variance-authority** - 樣式變體管理
- **clsx** & **tailwind-merge** - CSS 類名工具
- **lucide-react** - Icon 圖示庫
- **tailwindcss** - CSS 框架
- **typescript** - 型別檢查

### 路由結構
- `/` - 首頁
- `/movies` - 電影列表
- `/cart` - 購物車

### 資料流
1. CSV 檔案位於 `public/data/`
2. 使用 `fetch()` 載入
3. `papaparse` 解析 CSV
4. 儲存至 `MovieContext`
5. 各頁面透過 `useMovieContext()` 使用

## 🎨 頁面預覽

### 首頁 (/)
- 歡迎標語
- 三大功能介紹卡片
- 使用說明步驟

### 電影列表 (/movies)
- 顯示所有電影卡片
- 電影海報、標題、年份、片長
- 類型標籤、分級、語言
- 載入 `movies.csv` 資料

### 購物車 (/cart)
- 購物車項目列表
- 總金額計算
- 清空購物車按鈕
- 送出訂單按鈕

## 🔧 下一步開發建議

1. **實作電影詳情頁**
   - 建立 `src/pages/MovieDetail.tsx`
   - 顯示電影完整資訊
   - 列出該電影的所有場次

2. **實作座位選擇**
   - 建立 `src/components/SeatMap.tsx`
   - 根據 `seatmap_id` 產生座位圖
   - 可點選座位

3. **完善購物車功能**
   - 加入場次到購物車
   - 編輯座位選擇
   - 刪除項目

4. **實作結帳流程**
   - 建立 `src/pages/Checkout.tsx`
   - 顯示訂單摘要
   - 確認送出

## 📚 學習資源

- [Vite 文檔](https://vitejs.dev/)
- [React Router v6](https://reactrouter.com/)
- [TailwindCSS](https://tailwindcss.com/)
- [Shadcn UI](https://ui.shadcn.com/)
- [TypeScript](https://www.typescriptlang.org/)

## 💡 開發提示

- 使用 `@/` 作為 `src/` 的別名
- Shadcn UI 組件在 `src/components/ui/`
- 使用 `cn()` 函數合併 className
- CSV 檔案透過 `fetch('/data/xxx.csv')` 載入
- 使用 `useMovieContext()` Hook 存取全域狀態

## 🎉 專案已就緒！

現在你可以開始開發電影訂票系統的核心功能了！

